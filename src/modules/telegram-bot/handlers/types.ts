export enum Command {
    Start = 'start',
    Help = 'help',
    List = 'list',
    Add = 'add',
}

export enum Action {
    SubscriptionDetail = 'subscription-detail',
    List = 'list',
    Remove = 'remove',
    Add = 'add',
    Chart = 'chart',
}
